# 启动流程

1. 读取 `VERSION` 文件获取当前系统版本号。
2. 读取 `CHANGELOG.md` 获取最新版本的更新说明。
3. Read 任务清单.md（只读未完成任务 `[ ]`，跳过已完成 `[x]`）。
4. 输出简报：当前系统版本及最新版本说明、今日日期、紧急任务、近期截止。
5. 询问：「今天有要分诊的内容吗？」
6. 有 → 启动分诊流程（/triage）；没有 → 开始工作。

注意：禁止运行外部脚本，直接读文件更快更省 token。
注意：分诊是每日第一优先动作，先处理输入再处理任务。
